Put public splash and brand images in this folder.

For the login splash side image, use a browser-safe file such as:

  splash-side.jpg
  splash-side.png
  splash-side.webp

Then set:

  NEXT_PUBLIC_SPLASH_IMAGE_SRC=/images/splash-side.jpg

Do not put private user images, secrets, IDs, or tester data in this public folder.
